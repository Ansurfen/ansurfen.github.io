---
import Box from "@/components/box/index.astro";
import BoxHeader from "@/components/box/header.astro";
import BoxTitle from "@/components/box/title.astro";
import BoxContent from "@/components/box/content.astro";
import type { Experience } from "@/lib/types";
import ExperienceItem from "./experience-item.astro";
import type { ExperienceTexts } from "@/lib/constants/i18n";

interface Props {
  text: ExperienceTexts;
  experiences: Experience[];
}

const { text, experiences } = Astro.props;
---

<Box>
  <BoxHeader>
    <BoxTitle> {text.name} </BoxTitle>
  </BoxHeader>
  <BoxContent className="px-0">
    {
      experiences.map(experience => {
        return (
          <ExperienceItem
            experience={experience}
            showMore={text.showMore}
            showLess={text.showLess}
          />
        );
      })
    }
  </BoxContent>
</Box>
