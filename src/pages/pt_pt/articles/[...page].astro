---
import { getArticlePaths } from "@/lib/articles";
import ArticlesLayout from "@/layouts/articles-layout.astro";

export const getStaticPaths = await getArticlePaths("ptPTBlog");

const { page } = Astro.props;
---

<ArticlesLayout
  collection="ptPTBlog"
  title="Blog partilhado"
  description="Partilha de artigos sobre desenvolvimento web, backend, full stack, etc."
  seriesTitle="Séries"
  categoriesTitle="Categorias"
  articlesTitle="Artigos"
  basePath="/pt_pt/articles"
  page={page}
/>
