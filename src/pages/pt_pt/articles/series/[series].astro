---
import { getSeriesPaths } from "@/lib/articles";
import SeriesLayout from "@/layouts/series-layout.astro";

export async function getStaticPaths() {
  return getSeriesPaths("ptPTBlog");
}

const { series } = Astro.params;

// Page properties
const title = series;
const description = `Explore a série ${series} do meu percurso na engenharia de software.`;
---

<SeriesLayout
  series={series}
  title={title}
  baseLink="/pt_pt/articles"
  description={description}
  collectionName="ptPTBlog"
  articlesTitle="Artigos"
/>
