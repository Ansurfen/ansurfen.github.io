---
import { getArticlePaths } from "@/lib/articles";
import ArticlesLayout from "@/layouts/articles-layout.astro";

export const getStaticPaths = await getArticlePaths("arSABlog");

const { page } = Astro.props;
---

<ArticlesLayout
  collection="arSABlog"
  title="مشاركة المدونة"
  description="يتم مشاركة المقالات المختلفة بشكل غير منتظم"
  seriesTitle="السلسلة"
  categoriesTitle="التصنيف"
  articlesTitle="المقالات"
  basePath="/ar_sa/articles"
  page={page}
/>
